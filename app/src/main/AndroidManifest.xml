<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Csapp">

        <activity android:name=".AgentsActivity" />
        <activity android:name=".AgentDetailActivity" />

        <activity android:name=".CratesActivity" />
        <activity android:name=".CrateDetailActivity" />

        <activity android:name=".HighlightDetailActivity" />

        <activity android:name=".StickersActivity" />

        <activity android:name=".StickerDetailActivity" />

        <activity android:name=".SkinDetailActivity" />



        <activity android:name=".HighlightsActivity" />

        <activity
            android:name=".SkinsActivity"
            android:exported="false" />

        <!-- TELA PRINCIPAL -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
